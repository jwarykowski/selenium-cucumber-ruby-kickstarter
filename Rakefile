require 'cucumber'
require 'cucumber/rake/task'
require 'yard'

########################### DEFAULT TASK ############################
desc "Runs the default task"
task :default do
  Cucumber::Rake::Task.new(:default) do |t|
    t.profile = 'default'
  end
end
